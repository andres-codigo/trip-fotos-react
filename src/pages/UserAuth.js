import userAuthStyles from './UserAuth.module.scss';

function UserAuth() {
	return (
		<section className={userAuthStyles.userAuthenticationContainer}>
			<h1>Welcome to the UserAuth Page</h1>
		</section>
	);
}

export default UserAuth;
